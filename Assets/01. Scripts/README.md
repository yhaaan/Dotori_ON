런타임 코드. 전체가 DOTORION.Runtime 어셈블리 하나로 컴파일된다.

바로 아래 단계까지만 번호를 붙이고, 그 안쪽(`03. Backend/Mock`, `04. Platform/Windows`)은 이름만 쓴다.
에디터 전용 코드는 여기가 아니라 `90. Editor`, 테스트는 `99. Tests`에 둔다. 런타임 코드는 UnityEditor를 참조하지 않는다.
